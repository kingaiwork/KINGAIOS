name: KINGAI OS Foundation CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: [self-hosted, linux, x64, kingai-vps]
    steps:
      - run: |
          echo 'This legacy workflow is retired in ZERO-BILL mode.'
          echo 'Use .github/workflows/zero-bill-vps.yml and scripts/zero-bill-vps-validate.sh instead.'
          exit 1
