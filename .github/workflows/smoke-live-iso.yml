name: KINGAI OS Server Live ISO Smoke

on:
  push:
    branches: [main]
    paths:
      - 'scripts/build-live-iso.sh'
      - 'scripts/build-rootfs.sh'
      - 'distro/packages/server.txt'
      - 'distro/overlay/**'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/smoke-live-iso.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  boot-server-amd64:
    runs-on: ubuntu-24.04
    timeout-minutes: 65
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false
      - name: Install image and VM tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y mmdebstrap ubuntu-keyring zstd squashfs-tools xorriso grub-pc-bin grub-efi-amd64-bin grub-common mtools qemu-system-x86 ovmf
      - name: Build root filesystem
        run: sudo --preserve-env=PATH,KINGAI_SKIP_ARCHIVE KINGAI_SKIP_ARCHIVE=1 bash scripts/build-rootfs.sh server amd64 dist
      - name: Verify no build-host ownership leaked
        run: |
          root=dist/rootfs-server-amd64
          leaked=$(sudo find "$root" -xdev -mindepth 1 -uid "$(id -u)" -print -quit)
          test -z "$leaked" || { echo "build-host UID leaked into rootfs content: $leaked"; exit 1; }
          test "$(sudo stat -c '%u:%g' "$root")" = "0:0"
      - name: Build hybrid Live ISO
        run: sudo --preserve-env=PATH bash scripts/build-live-iso.sh server dist
      - name: Validate ISO filesystem checksums and SBOM
        run: |
          iso=$(find dist -maxdepth 1 -name '*.iso' -print -quit)
          test -s "$iso"
          xorriso -indev "$iso" -find /casper -type f -exec lsdl --
          xorriso -indev "$iso" -find /md5sum.txt -type f -exec lsdl --
          test -f "$iso.sha256"
          test -f "$iso.manifest.json"
          test -f "$iso.spdx.json"
          sha256sum -c "$iso.sha256"
      - name: QEMU BIOS boot smoke
        shell: bash
        run: |
          iso=$(find dist -maxdepth 1 -name '*.iso' -print -quit)
          set +e
          timeout 150 qemu-system-x86_64 -m 2048 -smp 2 -cdrom "$iso" -boot d -display none -serial stdio -no-reboot -snapshot > qemu-bios.log 2>&1
          code=$?
          set -e
          cat qemu-bios.log
          if [[ $code -ne 0 && $code -ne 124 ]]; then exit "$code"; fi
          grep -Eiq 'Linux version|casper|KINGAI|systemd' qemu-bios.log
          grep -Fq 'KINGAI OS 0.1 Developer Foundation' qemu-bios.log
          if grep -Eiq "Failed to start casper-md5check|can't execute '/usr/sbin/install-keymap'|update-notifier/.+No such file|var/crash/crash.init.+No such file" qemu-bios.log; then
            echo 'BIOS live-session compatibility regression detected.' >&2
            exit 1
          fi
      - name: QEMU UEFI boot smoke
        shell: bash
        run: |
          iso=$(find dist -maxdepth 1 -name '*.iso' -print -quit)
          code_file=/usr/share/OVMF/OVMF_CODE_4M.fd
          vars_file=/usr/share/OVMF/OVMF_VARS_4M.fd
          if [[ ! -f "$code_file" || ! -f "$vars_file" ]]; then
            code_file=/usr/share/OVMF/OVMF_CODE.fd
            vars_file=/usr/share/OVMF/OVMF_VARS.fd
          fi
          test -f "$code_file"
          test -f "$vars_file"
          cp "$vars_file" ovmf-vars.fd
          set +e
          timeout 150 qemu-system-x86_64 -machine q35 -m 2048 -smp 2 \
            -drive if=pflash,format=raw,readonly=on,file="$code_file" \
            -drive if=pflash,format=raw,file=ovmf-vars.fd \
            -cdrom "$iso" -boot d -display none -serial stdio -no-reboot -snapshot > qemu-uefi.log 2>&1
          code=$?
          set -e
          cat qemu-uefi.log
          if [[ $code -ne 0 && $code -ne 124 ]]; then exit "$code"; fi
          grep -Eiq 'Linux version|casper|KINGAI|systemd' qemu-uefi.log
          grep -Fq 'KINGAI OS 0.1 Developer Foundation' qemu-uefi.log
      - name: Report ISO size
        run: |
          iso=$(find dist -maxdepth 1 -name '*.iso' -print -quit)
          bytes=$(stat -c '%s' "$iso")
          echo "ISO bytes: $bytes"
          if (( bytes >= 2147483648 )); then echo "Release routing: R2"; else echo "Release routing: eligible for GitHub artifact/release"; fi
