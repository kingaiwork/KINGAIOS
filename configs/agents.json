{
  "agents": [
    {
      "id": "main",
      "role": "orchestrator",
      "capabilities": ["filesystem.read", "network.read", "filesystem.write"]
    },
    {
      "id": "system-ops",
      "role": "system-operations",
      "capabilities": ["filesystem.read", "network.read", "process.execute", "service.restart", "package.install", "network.modify", "boot.modify", "device.*"]
    },
    {
      "id": "sec-ops",
      "role": "security-operations",
      "capabilities": ["filesystem.read", "network.read", "audit.read", "security.modify", "network.modify"]
    }
  ],
  "rule": "Agent manifests never override the central policy engine. device.* only makes system-ops eligible for exact Device Pack capabilities that are loaded into Policy at runtime; privileged identities remain locally bound and higher-risk capabilities still require policy approval."
}
